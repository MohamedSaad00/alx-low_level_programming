#include "main.h"

/**
 * _putchar - that prints the numbers, from 0 to 9, followed by a new line.
 *
 * No Return (void)
 */
void print_numbers(void)
{
int num = 48;
for (num = 48; num < 58; num++)
{
_putchar(num);
}
_putchar(10);
}
